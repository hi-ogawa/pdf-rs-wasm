diff --git a/Cargo.toml b/Cargo.toml
index b8427da..28cf22a 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -17,3 +17,5 @@ tokio = { version = "1.18.2", optional = true, features = ["sync", "rt", "time"]
 tracing = { version = "0.1.34", optional = true }
 once_cell = { version = "1.10.0", optional = true }
 async-trait = "0.1.53"
+# TODO: why is this feature not automatic? https://github.com/sebcrozet/instant/issues/39
+instant = { version = "0.1", features = ["wasm-bindgen"] }
diff --git a/src/sync.rs b/src/sync.rs
index cb80b55..ebfc03b 100644
--- a/src/sync.rs
+++ b/src/sync.rs
@@ -2,7 +2,7 @@ use std::sync::{Mutex, Condvar, Arc, MutexGuard};
 use std::collections::hash_map::{HashMap, Entry};
 use std::hash::Hash;
 use std::mem::replace;
-use std::time::{Instant, Duration};
+use instant::{Instant, Duration};
 use super::{ValueSize, CacheControl, global::GlobalCache};
 use async_trait::async_trait;
 
@@ -32,7 +32,7 @@ struct CacheInner<K, V> {
     entries: HashMap<K, Value<V>>,
 }
 
-impl<K, V> SyncCache<K, V> 
+impl<K, V> SyncCache<K, V>
     where K: Hash + Eq + Clone + Send + 'static, V: Clone + ValueSize + Send + 'static,
 {
     pub fn new() -> Arc<Self> {

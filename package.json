{
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "tsc": "tsc -b packages/wasm",
    "lint": "run-s lint:*",
    "lint-check": "run-s lint-check:*",
    "lint:prettier": "prettier --write --cache .",
    "lint-check:prettier": "prettier --check --cache .",
    "lint:cargo-fmt": "cargo fmt --all",
    "lint-check:cargo-fmt": "cargo fmt --all --check"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.1",
    "esbuild": "^0.16.15",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.1",
    "tsup": "^7.2.0",
    "typescript": "^5.1.3",
    "vitest": "^0.26.3"
  },
  "volta": {
    "node": "16.19.0"
  }
}
